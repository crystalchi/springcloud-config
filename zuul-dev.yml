management:
  endpoints:
    web:
      exposure:
        include: routes,refresh

zuul:
  routes:
    api-a:
      path: /api-a/**
      serviceId: service-ribbon
    api-b:
      path: /api-c/**
      serviceId: service-feign